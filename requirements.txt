# Core web framework
streamlit==1.32.2            # UI framework for interactive dashboards

# Data handling
pandas==2.2.1                 # For dataframes, analysis, and visualizations
numpy==1.26.4                 # Used for numerical operations and ML scoring
python-docx==1.1.0           # To extract text from .docx files
python-dotenv==1.0.1         # For loading environment variables from .env
uuid==1.30                   # To generate unique ticket IDs (UUID-based)

# PDF and document parsing
PyMuPDF==1.23.24              # Used as 'fitz' for PDF text extraction

# Visualization
plotly==5.20.0                # For interactive charts and graphs

# NLP / LLM & RAG
langchain==0.1.16             # Base framework for chaining LLM and RAG components
langchain-community==0.0.33  # For Chroma, PyPDFLoader, and HuggingFace embeddings
langchain-openai==0.1.6       # For using Groq/OpenAI-compatible LLMs
sentence-transformers==2.6.1 # Needed by HuggingFace Embeddings
chromadb==0.4.24              # For persistent vector store (RAG memory)
tiktoken==0.6.0              # Optional, helps with token counting for LLMs

# HTTP requests
requests==2.31.0              # For fraud ticket API calls (mock API)

# JSON manipulation & safe evaluation
asttokens==2.4.1             # Required for ast.literal_eval parsing safety

# Backend LLM calls
groq==0.5.0                   # Python SDK to connect to Groq LLMs (llama3)

# Optional dev tools / static typing (safe fallback)
typing-extensions==4.11.0    # For runtime type safety across modules
